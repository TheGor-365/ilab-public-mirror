<% generation = Generation.new %>

<%= form_with(model: generation, html: { multipart: true }) do |form| %>

  <div class="row p-5 shadow bg-white mb-5">

    <%= form.label :features, class: 'anchorjs-link display-6 mb-5', id: 'generation-features' %>
    <%= form.rich_text_area :features, class: 'anchorjs-link form-control' %>
  </div>
<% end %>
