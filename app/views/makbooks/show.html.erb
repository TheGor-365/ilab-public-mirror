<% provide :page_title, @makbook.title %>
<% currently_at 'MacBooks' %>

<div class="row">

  <div class="col-2">
    <%= render 'makbooks_show_nav' %>
  </div>

  <div class="col-6 m-5">

    <div class="row p-5 shadow bg-white mb-5">
      <p class="align-middle">
        <span id="gadget" class="anchorjs-link display-2"><%= @makbook.title %>
          <span class="text-muted display-2">
            <strong>show room</strong>
          </span>
        </span>
      </p>
    </div>

    <div class="row mt-5 mb-5 p-5 shadow bg-white">
      <span class="display-6 mb-3"><%= @makbook.title %>
        <span id="overview" class="anchorjs-link text-muted display-6">
          info
        </span>
      </span>

      <small>
        <div class="row">
          <div class="col-4">
            <span class="text-muted">
              Diagonal:<br>
              Model:<br>
              Version:<br>
              Series:<br>
              Production period:<br>
              Full title:<br>
            </span>
          </div>
          <div class="col-6">
            <%= @makbook.diagonal.size == 0 ? "#{'no info'}" : @makbook.diagonal %><br>
            <%= @makbook.model.size == 0 ? "#{'no info'}" : @makbook.model %><br>
            <%= @makbook.version.size == 0 ? "#{'no info'}" : @makbook.version %><br>
            <%= @makbook.series.size == 0 ? "#{'no info'}" : @makbook.series %><br>
            <%= @makbook.production_period %><br>
            <%= @makbook.full_title.size == 0 ? "#{'no info'}" : @makbook.full_title %>
          </div>
        </div>
      </small>
    </div>

    <div class="row p-5 shadow bg-white">
      <span class="display-6 mb-3"><%= @makbook.title %>
        <span id="photos" class="anchorjs-link text-muted display-6">
          photos
          <a href="#photos"></a>
        </span>
      </span>
      <br>

      <% @makbook.images.each do |image| %>
        <div class="col text-center mb-5">
          <%= image_tag(image.url, width: '300', class: 'rounded img-thumbnail shadow') %>
        </div>
      <% end %>
    </div>

    <div class="row mt-5 mb-2 p-5 shadow bg-white">
      <span class="display-6 mb-3"><%= @makbook.title %>
        <span id="overview" class="anchorjs-link text-muted display-6">
          overview
        </span>
      </span>
      <%= @makbook.overview %>
    </div>

    <div class="row mt-5 p-5 mb-4 bg-white shadow">
      <span class="display-6 mb-3"><%= @makbook.title %>
        <span id="videos" class="anchorjs-link text-muted display-6">
          videos
        </span>
      </span>

      <% @makbook.videos.each do |video| %>
        <small class="text-muted mb-5"><%= video.identifier %></small>
      <% end %>

      <div class="col">
        <%= video_tag(@makbook.videos, width: '640', height: '380', controls: true, class: 'rounded card p-1 shadow mx-auto mb-5') if @makbook.videos.present? %>
      </div>
    </div>

    <div class="list-inline-item mt-3">
      <%= link_to 'Edit', edit_makbook_path(@makbook), class: 'text-decoration-none me-2' %>
      <%= link_to 'Back', makbooks_path, class: 'text-decoration-none' %>
    </div>
  </div>

  <div class="col-3">
    <%= render 'makbooks_info_card' %>
  </div>
</div>
