<% provide :page_title, @apple_watch.title %>
<% currently_at 'Apple Watches' %>

<div class="row">

  <div class="col-2">
    <%= render 'apple_watch_show_nav' %>
  </div>

  <div class="col-6 m-5">

    <div class="row p-5 shadow bg-white mb-5">
      <p class="align-middle">
        <span id="gadget" class="anchorjs-link display-2"><%= @apple_watch.title %>
          <span class="text-muted display-2">
            <strong>show room</strong>
          </span>
        </span>
      </p>
    </div>

    <div class="row mt-5 mb-5 p-5 shadow bg-white">
      <span class="display-6 mb-3"><%= @apple_watch.title %>
        <span id="overview" class="anchorjs-link text-muted display-6">
          info
        </span>
      </span>

      <small>
        <div class="row">
          <div class="col-4">
            <span class="text-muted">
              Diagonal:<br>
              Model:<br>
              Version:<br>
              Series:<br>
              Production period:<br>
              Full title:<br>
            </span>
          </div>
          <div class="col-6">
            <%= @apple_watch.diagonal.size == 0 ? "#{'no info'}" : @apple_watch.diagonal %><br>
            <%= @apple_watch.model.size == 0 ? "#{'no info'}" : @apple_watch.model %><br>
            <%= @apple_watch.version.size == 0 ? "#{'no info'}" : @apple_watch.version %><br>
            <%= @apple_watch.series.size == 0 ? "#{'no info'}" : @apple_watch.series %><br>
            <%= @apple_watch.production_period %><br>
            <%= @apple_watch.full_title.size == 0 ? "#{'no info'}" : @apple_watch.full_title %>
          </div>
        </div>
      </small>
    </div>

    <div class="row p-5 shadow bg-white">
      <span class="display-6 mb-3"><%= @apple_watch.title %>
        <span id="photos" class="anchorjs-link text-muted display-6">
          photos
          <a href="#photos"></a>
        </span>
      </span>
      <br>

      <% @apple_watch.images.each do |image| %>
        <div class="col text-center mb-5">
          <%= image_tag(image.url, width: '300', class: 'rounded img-thumbnail shadow') %>
        </div>
      <% end %>
    </div>

    <div class="row mt-5 mb-2 p-5 shadow bg-white">
      <span class="display-6 mb-3"><%= @apple_watch.title %>
        <span id="overview" class="anchorjs-link text-muted display-6">
          overview
        </span>
      </span>
      <%= @apple_watch.overview %>
    </div>

    <div class="row mt-5 p-5 mb-4 bg-white shadow">
      <span class="display-6 mb-3"><%= @apple_watch.title %>
        <span id="videos" class="anchorjs-link text-muted display-6">
          videos
        </span>
      </span>

      <% @apple_watch.videos.each do |video| %>
        <small class="text-muted mb-5"><%= video.identifier %></small>
      <% end %>

      <div class="col">
        <%= video_tag(@apple_watch.videos, width: '640', height: '380', controls: true, class: 'rounded card p-1 shadow mx-auto mb-5') if @apple_watch.videos.present? %>
      </div>
    </div>

    <div class="list-inline-item mt-3">
      <%= link_to 'Edit', edit_apple_watch_path(@apple_watch), class: 'text-decoration-none me-2' %>
      <%= link_to 'Back', apple_watches_path, class: 'text-decoration-none' %>
    </div>
  </div>

  <div class="col-3">
    <%= render 'apple_watch_info_card' %>
  </div>
</div>
