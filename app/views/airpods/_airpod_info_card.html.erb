<div class="fixed-right shadow bg-white p-4 pe-3 col-3 m-3 mt-5">
  <h4 class="text-center">
    Photo <span class="text-muted"><%= @airpod.title %></span>
  </h4>
  <br>

  <%= image_tag(@airpod.avatar.url, width: '420') if @airpod.avatar.present? %>
</div>
